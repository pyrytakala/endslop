OPENAI_API_KEY=
FIREWORKS_API_KEY=
SUPADATA_API_KEY=
TRANSCRIPTAPI_API_KEY=

# Optional: supadata | transcriptapi (defaults to transcriptapi when its key is set)
TRANSCRIPT_PROVIDER=

# Vercel deploy (create at https://vercel.com/account/tokens — must be a personal access token, prefix vcp_)
VERCEL_TOKEN=
